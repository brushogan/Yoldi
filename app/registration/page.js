import React from "react";
import AuthForm from "../components/UI/authForm";

export const metadata = {
  title: "Registration",
};

const Login = () => <AuthForm type="sign-up" />;

export default Login;
